// export const createUrl = (value: string, target : string) => {
//     const params = new URLSearchParams(searchParams.toString());
//     params.set(target, value);
//     return `/?${params.toString()}`;
// };

// export default createUrl;
