'use client';
import Modal from '../ui/Modal';

const RecommendationModal = () => {
    return (
        <Modal isOpen={true} onClose={() => {}}>
            <h1>RecommendationModal</h1>
        </Modal>
    );
};

export default RecommendationModal;
